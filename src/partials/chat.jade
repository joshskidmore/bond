div(ng-controller='ChatCtrl')

	#side-menu(ng-controller='ContactsCtrl')
		h3 Contacts

		a(href='', ng-repeat='contact in contacts')
			i.glyphicon.glyphicon-user
			span {{ contact.name }}

	.container
		h1 Bond
